# DevRecall — hackathon prototype

## Run it locally

```bash
npm install
npm run dev
```

Then open the printed local URL (usually http://localhost:5173) in your browser.
Resize the window or open dev tools' device toolbar to see the mobile layout
(bottom nav, phone-frame capture screens).

## Build for deployment

```bash
npm run build
npm run preview
```
